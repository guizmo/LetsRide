<!--
  Generated template for the SearchPage page.

  See http://ionicframework.com/docs/components/#navigation for more info on
  Ionic pages and navigation.
-->
<ion-header>

  <ion-navbar>
    <button ion-button menuToggle>
      <ion-icon name="menu"></ion-icon>
    </button>
    <ion-title>Find riders</ion-title>
  </ion-navbar>


  <ion-toolbar>
    <ion-searchbar
      (ionInput)="search($event)"
      [showCancelButton]=true
      (ionCancel)="onCancel($event)"
      [debounce]="500"
      placeholder="Find a rider"
      #searchInput>
    </ion-searchbar>
  </ion-toolbar>
</ion-header>


<ion-content #searchContent>

  <!-- <ion-list>
    <ng-container *ngFor="let user of usersList | orderBy : ['sortByName']; let i=index">
      <ion-item *ngIf="i<limit.value && currentUser && user.aFuid != currentUser.uid ">
        <ion-avatar item-start>
          <img src="{{user.avatar}}">
        </ion-avatar>
        <h2>{{user.settings.displayName}}</h2>
        <h3 *ngIf="user.settings.country">{{user.settings.country}}</h3>
        <button
          *ngIf="!user.buddies || !user.buddies[currentUser.uid]"
          ion-button clear item-end icon-right
          (click)="sendFriendRequest(user.$key, user.oneSignalId, user.settings.displayName)" >
          Add rider
          <ion-icon color="dark" name="person-add"></ion-icon>
        </button>
      </ion-item>
    </ng-container>
  </ion-list> -->
  <ion-list>
    <ng-container *ngFor="let person of people">
      <ion-item>
        <ion-avatar item-start>
          <img src="{{person.avatar}}">
        </ion-avatar>
        <h2>{{person.settings.displayName}}</h2>
        <h3 *ngIf="person.settings.country">{{person.settings.country}}</h3>
        <button
          *ngIf="!person.buddies || !person.buddies[currentUser.uid]"
          ion-button clear item-end icon-right
          (click)="sendFriendRequest(person.$key, person.oneSignalId, person.settings.displayName)" >
          Add rider
          <ion-icon color="dark" name="person-add"></ion-icon>
        </button>
      </ion-item>
    </ng-container>
  </ion-list>

  <ion-infinite-scroll (ionInfinite)="scrolled($event)">
    <ion-infinite-scroll-content></ion-infinite-scroll-content>
  </ion-infinite-scroll>

</ion-content>
